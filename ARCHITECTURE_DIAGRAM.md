# AIChat Architecture Visual Guide

## 🏗️ Feature Structure Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      AIChat/Core/Feature/                        │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────┐
│   FeatureView.swift  │  ← SwiftUI UI Layer
│   @State viewModel   │  ← Observes ViewModel
└──────────┬───────────┘
           │ User Actions
           ↓
┌──────────────────────┐
│ FeatureViewModel.swift│  ← Presentation Layer
│ @Observable @MainActor│  ← UI State Management
└──────────┬───────────┘
           │ Business Logic Calls
           ↓
┌──────────────────────┐
│ FeatureUseCase.swift │  ← Business Logic Layer
│ Protocol-based       │  ← Accesses Managers
└──────────┬───────────┘
           │ Navigation Requests
           ↓
┌──────────────────────┐
│ FeatureRouter.swift  │  ← Navigation Layer
│ Protocol + Impl      │  ← Pushes/Presents Views
└──────────┬───────────┘
           │ View Construction
           ↓
┌──────────────────────┐
│ FeatureBuilder.swift │  ← Dependency Injection
│ Wires everything up  │  ← Service Locator Pattern
└──────────────────────┘
```

## 🔄 Data Flow Diagram

```
User Interaction
       │
       ↓
┌─────────────────────────────────────────────────────────────────┐
│                            VIEW                                  │
│  • Captures user input (taps, gestures, text)                   │
│  • Displays UI based on ViewModel state                         │
│  • Pure SwiftUI - no business logic                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ viewModel.onActionPressed()
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                         VIEW MODEL                               │
│  • Handles user actions                                         │
│  • Manages UI state (@Published properties)                     │
│  • Calls UseCase for business logic                             │
│  • Triggers navigation via Router                               │
│  • Tracks analytics events                                      │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ useCase.performAction()
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                          USE CASE                                │
│  • Implements business logic                                    │
│  • Resolves managers from DependencyContainer                   │
│  • Coordinates between multiple managers                        │
│  • No UI concerns - pure business logic                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ manager.apiCall()
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                          MANAGERS                                │
│  • AIManager, UserManager, ChatManager, etc.                    │
│  • Protocol-based (multiple implementations)                    │
│  • Registered in DependencyContainer                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ service.request()
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                          SERVICES                                │
│  • External API calls (OpenAI, Firebase, etc.)                  │
│  • Multiple implementations (Production, Mock)                  │
│  • Configured per build scheme                                  │
└─────────────────────────────────────────────────────────────────┘
                         │
                         │ Response
                         ↓
                    (Flow back up)
```

## 🏢 Dependency Injection Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      DependencyContainer                         │
│                    (Service Locator)                             │
│                                                                  │
│  ┌────────────────────────────────────────────────┐            │
│  │         Registered Dependencies                │            │
│  │                                                │            │
│  │  • AIManager          → ProductionAIServer    │            │
│  │  • AuthManager        → FirebaseAuthService   │            │
│  │  • UserManager        → UserService           │            │
│  │  • ChatManager        → ChatService           │            │
│  │  • LogManager         → ConsoleService        │            │
│  │  • AnalyticsManager   → MixpanelService       │            │
│  │  • ...more managers                            │            │
│  └────────────────────────────────────────────────┘            │
└─────────────────────────┬───────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          │               │               │
          ↓               ↓               ↓
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │ UseCase1 │    │ UseCase2 │    │ UseCase3 │
    └──────────┘    └──────────┘    └──────────┘
          │               │               │
          │ resolve()     │ resolve()     │ resolve()
          └───────────────┴───────────────┘
```

## 🎯 Builder Pattern Flow

```
                    Application Startup
                            │
                            ↓
                  ┌──────────────────┐
                  │  AIChatApp.swift │
                  └────────┬─────────┘
                           │
                           ↓
              ┌─────────────────────────┐
              │  Create Container       │
              │  Register Dependencies  │
              └────────┬────────────────┘
                       │
                       ↓
         ┌─────────────────────────────────┐
         │  Create Root Builder            │
         │  (e.g., AppViewBuilder)         │
         └────────┬────────────────────────┘
                  │
                  ↓
    ┌──────────────────────────────────────────┐
    │  Builder.buildView(router: Router)       │
    │                                           │
    │  1. Create UseCase(container)            │
    │  2. Create Router(router)                │
    │  3. Create ViewModel(useCase, router)    │
    │  4. Create View(viewModel)               │
    │  5. Return View                          │
    └──────────────────────────────────────────┘
```

## 📦 Module Organization

```
AIChat/
│
├── App/
│   ├── AIChatApp.swift          ← Entry point
│   ├── DependencyContainer.swift ← Service locator
│   └── Dependencies.swift        ← Dependency registration
│
├── Core/                         ← Feature modules
│   ├── About/
│   │   ├── AboutView.swift
│   │   ├── AboutViewModel.swift
│   │   ├── AboutUseCase.swift
│   │   ├── AboutBuilder.swift
│   │   └── AboutRouter.swift
│   │
│   ├── Profile/
│   │   ├── ProfileView.swift
│   │   ├── ProfileViewModel.swift
│   │   ├── ProfileUseCase.swift
│   │   ├── ProfileBuilder.swift
│   │   └── ProfileRouter.swift
│   │
│   └── ... (15+ features)
│
├── Services/                     ← External service integrations
│   ├── AI/
│   │   ├── AIManager.swift      ← Protocol
│   │   ├── OpenAIServer.swift   ← Production
│   │   └── MockAIServer.swift   ← Mock
│   │
│   ├── Auth/
│   ├── User/
│   ├── Chat/
│   └── ... (more services)
│
├── Components/                   ← Reusable UI components
├── Utilities/                    ← Helper functions
└── Assets.xcassets/             ← Visual assets
```

## 🔧 Build Configuration Flow

```
                    Xcode Scheme Selection
                            │
          ┌─────────────────┼─────────────────┐
          │                 │                 │
          ↓                 ↓                 ↓
    ┌──────────┐     ┌──────────┐     ┌──────────┐
    │   Mock   │     │   Dev    │     │   Prod   │
    └────┬─────┘     └────┬─────┘     └────┬─────┘
         │                │                │
         ↓                ↓                ↓
┌─────────────────────────────────────────────────────┐
│           Dependencies.swift                         │
│                                                      │
│  #if MOCK_TARGET                                    │
│      register(MockAIServer())                       │
│  #elseif DEV_TARGET                                 │
│      register(OpenAIServer())                       │
│      register(ConsoleService())                     │
│  #else // PROD_TARGET                               │
│      register(OpenAIServer())                       │
│      // No console logging                          │
│  #endif                                             │
└─────────────────────────────────────────────────────┘
```

## 🧪 Test Architecture

```
Production Code                    Test Code
─────────────────────────────────────────────────────
┌──────────────────┐            ┌──────────────────┐
│  FeatureView     │            │  ViewTests       │
└────────┬─────────┘            └────────┬─────────┘
         │                               │
┌────────▼─────────┐            ┌────────▼─────────┐
│ FeatureViewModel │            │ ViewModelTests   │
└────────┬─────────┘            └────────┬─────────┘
         │                               │
┌────────▼─────────┐            ┌────────▼─────────┐
│ FeatureUseCase   │            │  UseCaseTests    │
│ (Protocol)       │            │  (Uses Mocks)    │
└────────┬─────────┘            └──────────────────┘
         │
┌────────▼─────────┐
│  Managers        │
│  (Protocol)      │
└────────┬─────────┘
         │
         ├─────────┬─────────┐
         ↓         ↓         ↓
    ┌────────┐ ┌────────┐ ┌────────┐
    │  Prod  │ │  Mock  │ │ Test   │
    │Service │ │Service │ │Service │
    └────────┘ └────────┘ └────────┘
```

## 🎨 Template Generation

```
Developer Action: New File from MVVMTemplate
         │
         ↓
┌─────────────────────────────────────────┐
│  Xcode Template System                  │
│                                          │
│  Input:                                  │
│  • Module Name: "UserProfile"           │
│  • camelCased Name: "userProfile"       │
│  • Core Router Name: "Core"             │
└────────┬────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│  Template Processing                    │
│  • Replace ___VARIABLE_productName___   │
│  • Replace ___VARIABLE_camelCased___    │
│  • Replace ___VARIABLE_coreName___      │
│  • Replace ___FILEHEADER___             │
└────────┬────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│  Generated Files:                        │
│  ✓ UserProfileView.swift                │
│  ✓ UserProfileViewModel.swift           │
│  ✓ UserProfileUseCase.swift             │
│  ✓ UserProfileBuilder.swift             │
│  ✓ UserProfileRouter.swift              │
└─────────────────────────────────────────┘
```

## 🔐 Protocol-Based Architecture

```
                    Protocol Layer
┌─────────────────────────────────────────────────────┐
│  protocol AIManagerProtocol {                       │
│      func sendMessage() async throws -> Response   │
│  }                                                  │
└─────────────┬───────────────────────────────────────┘
              │
              │ Conformance
              │
    ┌─────────┴─────────┬─────────────┐
    │                   │             │
    ↓                   ↓             ↓
┌─────────┐      ┌──────────┐   ┌────────┐
│Production│      │   Mock   │   │  Test  │
│ Server  │      │  Server  │   │ Server │
└─────────┘      └──────────┘   └────────┘

Benefits:
✅ Easy to swap implementations
✅ Testable (inject mocks)
✅ Build configuration flexibility
✅ Adheres to SOLID principles
```

## 🎯 Key Takeaways

1. **Clean Separation**: Each layer has a single responsibility
2. **Protocol-Based**: All services are protocol-based for flexibility
3. **Dependency Injection**: Container manages all dependencies
4. **Builder Pattern**: Centralizes view construction
5. **Testability**: Mock implementations for all services
6. **Scalability**: Easy to add new features with template

---

**This architecture ensures:**
- ✅ Maintainable codebase
- ✅ Testable components
- ✅ Consistent patterns
- ✅ Easy onboarding for new developers
- ✅ Flexible service implementations
